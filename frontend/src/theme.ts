import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      // white
      main: '#000000',
    },
    secondary: {
      // green
      main: '#1cd760',
    },
    background: {
      // grey
      default: '#88888b',
    },
  },
});
